name: Bulletin Scraper Auto-Update

on:
  workflow_dispatch:
    inputs:
      create_pr:
        description: Create a pull request with the applied changes
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      targets:
        description: Targets to run
        required: false
        default: schedule,events,intentions
        type: string
      strategies:
        description: Strategies to evaluate before selecting the best result
        required: false
        default: direct,extract-merge,reviewed
        type: string
      input_modes:
        description: Bulletin input modes to evaluate
        required: false
        default: images,text
        type: string
      family_filter:
        description: Restrict the run to families whose id or name contains this value
        required: false
        default: ""
        type: string
      family_limit:
        description: Limit the run to the first N families
        required: false
        default: ""
        type: string
      workers:
        description: Family-level parallel worker count
        required: false
        default: "4"
        type: string
      log_level:
        description: Logging level
        required: false
        default: INFO
        type: choice
        options:
          - DEBUG
          - INFO
          - WARNING
          - ERROR
      use_existing_bulletins:
        description: Force reuse of local bulletin PDFs
        required: false
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      refresh_bulletins:
        description: Ignore same-day cache entries and rescrape bulletins
        required: false
        default: "false"
        type: string
      model:
        description: OpenRouter model to use
        required: false
        default: google/gemini-3.1-flash-lite-preview
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  scrape-analyze-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6

      - name: Sync dependencies
        run: uv sync --all-groups

      - name: Run bulletin analysis with apply
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPENROUTER_MODEL: ${{ github.event.inputs.model }}
        run: |
          args=(
            --targets "${{ github.event.inputs.targets }}"
            --strategies "${{ github.event.inputs.strategies }}"
            --input-modes "${{ github.event.inputs.input_modes }}"
            --workers "${{ github.event.inputs.workers }}"
            --log-level "${{ github.event.inputs.log_level }}"
            --model "${{ github.event.inputs.model }}"
            --apply
          )

          if [ -n "${{ github.event.inputs.family_filter }}" ]; then
            args+=(--family-filter "${{ github.event.inputs.family_filter }}")
          fi

          if [ -n "${{ github.event.inputs.family_limit }}" ]; then
            args+=(--family-limit "${{ github.event.inputs.family_limit }}")
          fi

          if [ "${{ github.event.inputs.use_existing_bulletins }}" = "true" ]; then
            args+=(--use-existing-bulletins)
          else
            args+=(--no-use-existing-bulletins)
          fi

          if [ "${{ github.event.inputs.refresh_bulletins }}" = "true" ]; then
            args+=(--refresh-bulletins)
          else
            args+=(--no-refresh-bulletins)
          fi

          uv run python app.py "${args[@]}"

      - name: Resolve latest run directory
        id: latest_run
        shell: bash
        run: |
          latest_run=$(find runs -mindepth 1 -maxdepth 1 -type d | sort | tail -n 1)
          if [ -z "$latest_run" ]; then
            echo "No run directory was produced"
            exit 1
          fi
          echo "path=$latest_run" >> "$GITHUB_OUTPUT"
          cp "$latest_run/summary.md" workflow-summary.md

      - name: Upload run artifacts
        uses: actions/upload-artifact@v4
        with:
          name: bulletin-run
          path: ${{ steps.latest_run.outputs.path }}

      - name: Check for repository changes
        id: check_changes
        shell: bash
        run: |
          if git diff --quiet -- data runs; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            git diff --stat -- data runs
          fi

      - name: Create Pull Request
        if: steps.check_changes.outputs.has_changes == 'true' && github.event.inputs.create_pr == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "chore: sync bulletin-derived data"
          title: "chore: sync bulletin-derived data"
          body-path: workflow-summary.md
          branch: bulletin-sync-${{ github.run_number }}
          delete-branch: true
          labels: |
            bulletin-sync
            automated
          add-paths: |
            data/*.json

      - name: Report no changes
        if: steps.check_changes.outputs.has_changes == 'false'
        run: echo "No data changes detected."